{
  "project_settings": {
    "numbers_to_predict": 5,
    "number_range": {
      "min": 1,
      "max": 25
    }
  },
  "strategies": {
    "detective": {
      "enabled": true,
      "weight": 1.2,
      "params": {
        "lookback_days": 10,
        "description": "Busca números que no han salido en los últimos 'lookback_days' días."
      }
    },
    "semaforo": {
      "enabled": true,
      "weight": 0.8,
      "params": {
        "hot_threshold_days": 5,
        "cold_threshold_days": 15,
        "description": "Clasifica números como calientes (muy frecuentes), fríos (poco frecuentes) o neutros."
      }
    },
    "persistencia": {
      "enabled": true,
      "weight": 1.0,
      "params": {
        "repeat_lookback": 3,
        "description": "Premia a los números que han aparecido en los últimos 'repeat_lookback' sorteos."
      }
    },
    "afinidad": {
      "enabled": false,
      "weight": 0.9,
      "params": {
        "description": "Busca pares de números que tienden a salir juntos."
      }
    }
  },
  "meta_strategy": {
    "description": "Combina los puntajes de las estrategias activas usando sus pesos.",
    "formula": "score_total = (score_detective * weight_detective) + (score_semaforo * weight_semaforo) + ..."
  }
}